# Prefer: pip install -e ".[test]"  (uses pyproject.toml)
# This file is kept for compatibility with pip install -r requirements.txt

python-dotenv>=1.0.0
requests>=2.31.0
scipy>=1.11.0
python-telegram-bot>=21.0

# Testing
pytest>=7.0
pytest-asyncio>=0.21

# Optional – only needed for --map (HTML map generation)
# folium>=0.15.0
